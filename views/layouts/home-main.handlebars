<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VietEdu Academy - Nền tảng học trực tuyến</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cabin:ital,wght@0,400..700;1,400..700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />

    <link href="https://unpkg.com/aos@next/dist/aos.css" rel="stylesheet">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">

    <!-- Mobile Responsive CSS -->
    <link rel="stylesheet" href="/mobile.css">

    <style>
        :root {
            /* Định nghĩa lại màu sắc của Bootstrap để hợp với thương hiệu */
            --bs-primary: #46935f;
            --bs-primary-rgb: 70, 147, 95;
            --bs-primary-dark: #347571; /* Màu này dùng cho h2, h1... */

            --bs-secondary: #ECF8E5; /* Nền xanh lá nhạt */
            --bs-light: #f7fbf5;     /* Nền trắng-xanh */
            
            --bs-font-sans-serif: 'Cabin', sans-serif;
            --bs-body-color: #2c3e50;
            --bs-body-line-height: 1.6;
        }



        body {
            /* Cần thiết cho scrollspy */
            position: relative; 
        }

        h1, h2, h3, h4, h5, h6 {
            color: var(--bs-primary-dark);
            font-weight: 600;
        }

        h2 {
            font-size: 2.5rem;
            font-weight: 700;
            position: relative;
        }

        /* Hiệu ứng gạch chân cho tiêu đề h2 */
        h2::after {
            content: '';
            display: block;
            width: 80px;
            height: 4px;
            background-color: var(--bs-primary);
            margin: 15px auto 0;
            border-radius: 2px;
        }

        .card {
            border-radius: 16px;
            border: 1px solid #dbeedb;
            background-color: var(--white);
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.04);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .card:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
        }

        .card-header {
            background-color: var(--green-primary);
            color: white;
            font-weight: 600;
            border-top-left-radius: 16px !important;
            border-top-right-radius: 16px !important;
        }
        
        /* Đảm bảo navbar không che mất nội dung */
        section {
            padding-top: 80px;
            padding-bottom: 80px;
        }
        #hero {
            padding-top: 160px; /* Hero cần nhiều khoảng trống hơn */
            padding-bottom: 120px;
        }

        /* Custom Card Hover */
        .category-card-hover {
            transition: all 0.3s ease;
        }
        .category-card-hover:hover {
            transform: translateY(-10px);
            box-shadow: var(--bs-box-shadow-lg) !important;
            background-color: var(--bs-light);
            color: var(--bs-primary);
        }

        /* Footer Link Hover */
        .footer-link {
            transition: all 0.3s ease;
        }
        .footer-link:hover {
            color: var(--bs-white) !important;
            opacity: 1 !important;
            padding-left: 5px;
        }
        
        /* Tùy chỉnh nút Swiper */
        .courses-swiper .swiper-button-prev,
        .courses-swiper .swiper-button-next {
            background: var(--bs-white);
            border-radius: 50%;
            box-shadow: var(--bs-box-shadow-sm);
            color: var(--bs-primary);
            width: 45px;
            height: 45px;
            transition: all 0.3s ease;
        }
        .courses-swiper .swiper-button-prev:hover,
        .courses-swiper .swiper-button-next:hover {
            background: var(--bs-primary);
            color: var(--bs-white);
        }
        .courses-swiper .swiper-button-next::after,
        .courses-swiper .swiper-button-prev::after {
            font-size: 1.2rem;
            font-weight: 700;
        }
        .swiper-pagination-bullet-active {
            background: var(--bs-primary) !important;
        }
        
        /* Tùy chỉnh nút Bootstrap Carousel (cho Testimonials) */
        #testimonial-swiper .carousel-control-prev-icon,
        #testimonial-swiper .carousel-control-next-icon {
             /* Dùng filter để đổi màu icon SVG mặc định */
            filter: invert(0.5) sepia(1) saturate(5) hue-rotate(90deg);
        }

        .star-rating i {
            display: inline-block; /* đảm bảo không xuống dòng */
            margin-right: 2px;     /* khoảng cách nhỏ giữa các sao */
            font-size: 1rem;       /* có thể tùy chỉnh */
            line-height: 1;        /* tránh lệch dọc */
        }

    </style>
</head>

<body data-bs-spy="scroll" data-bs-target="#mainNavbar" data-bs-offset="100">
{{{body}}}

<footer class="footer bg-dark text-white pt-5 pb-4">
    <div class="container py-4">
    <div class="row">
        <div class="col-md-8">
        <h5 class="mb-2">About VietEdu Academy</h5>
        <p class="small mb-2">
            VietEdu Academy provides practical, project-based online courses to help learners upskill in
            programming, data, and business.
        </p>
        <p class="small mb-0">
            Our mission is to make quality education accessible to everyone.
        </p>
        </div>
        <div class="col-md-4 text-md-end mt-3 mt-md-0">
        <h6 class="mb-2">Contact</h6>
        <p class="mb-1"><a href="/contact" class="text-decoration-none" style="color: var(--bs-secondary)">Contact Us</a></p>
        <p class="mb-1 small">
            Address:
            <a href="https://www.google.com/maps/place/Tr%C6%B0%E1%BB%9Dng+%C4%90%E1%BA%A1i+h%E1%BB%8Dc+S%C6%B0+ph%E1%BA%A1m+K%E1%BB%B9+thu%E1%BA%ADt+Th%C3%A0nh+ph%E1%BB%91+H%E1%BB%93+Ch%C3%AD+Minh/"
            target="_blank"
            class="text-decoration-none"
            style="color: var(--bs-secondary)">Số 1 Võ Văn Ngân, Thủ Đức, TP. HCM</a>
        </p>
        <p class="mb-1"><a href="/sitemap" class="text-decoration-none" style="color: var(--bs-secondary)">Where do you want to go?</a></p>
    <p class="text-muted small mb-0">© {{currentYear}} VietEdu Academy</p>
        </div>
    </div>
    </div>
</footer>

</body>
</html>